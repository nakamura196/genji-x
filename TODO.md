# 残件

**このファイルには未対応のものだけを書く。** 済んだものは `DONE.md` へ移す。
以前は同じ章に `[ ]` と `[x]` が混ざっていて、何が残っているか読み取れなかった。

進め方は `CLAUDE.md`、用語と移植の指針は `GUIDELINES.md`。

## 見た目・使い勝手

- [ ] **Hero の検索フォームのアイコンと配置がいまいち**
      ヘッダ用の SearchBar CSS を流用したため、ボタンが入力欄を覆う配置
      （position:absolute で上下左右 1px）になり、高さも 38px 固定。Hero には合わない
- [ ] **favicon がテンプレートのまま**
      `src/app/icon.svg`。ロゴと同じ図案に揃える
- [ ] **ナビが 1440px で下段に回り、中央に浮く**
      本家の分岐が 94rem (1504px)。本家はナビ項目が多いが、こちらは 2 つだけなので
      不自然。項目数に合わせて分岐を下げる（例 64rem）。**要判断**
- [ ] **ロゴが崩れて見える**
      自作の SVG（源氏香を模した縦線）が、線 3 本の断片に見える。作り直す
- [ ] **Hero の検索欄にプレースホルダが出ない**
      ヘッダ用の SearchBar CSS をそのまま流用し、`padding-left: var(--button-size)` で
      文字が隠れている。Hero 用の見た目を別に作る
- [ ] **ナビが 1440px で下段に回る**
      本家の分岐が 94rem (1504px) なので本家どおりだが、不自然。要判断
- [ ] **リンクが本文と区別できない**
      色だけで区別している。WCAG 1.4.1（色だけに頼らない）違反。下線を付ける。
      axe の link-in-text-block が検出する規則
- [ ] **アクセント色を AA 適合値に差し替える**
- [ ] `/ja` の区画の間隔が 0px
- [ ] テンプレートの残骸 5 件（`Common.title` が「Next.js 国際化テンプレート」等）
- [ ] プレースホルダが実際に見えている（入力欄の中に描画領域がある）
- [ ] ドロップダウンが画面内に収まる（開いた後の右端 <= viewport 幅）
- [ ] ヘッダの操作が 1 行に収まる（y 座標が揃っている）
- [ ] ロゴに実体がある（bounding box が一定以上）

## データと帰属

- [ ] **画面の「公開者」を「この記録の公開者」に変え、資料の作成主体を別に出す**
- [ ] **構造化データで sdPublisher と creator を分ける**
- [ ] **団体名（裏源氏勉強会 等）を名乗るなら、まず TEI ヘッダに入れる**
      TEI にもサイトにも現在は記載が無い。`<authority>` か `<respStmt>` に入れれば
      この目録も自動的に追従する。順番は
      (1) TEI に入れる → (2) 01-digest で root を出し直す → (3) DDO を更新。
      **(2) で root が変わる**ので、いまの root で記録した分との関係を記事の材料にできる
- [ ] **宣言の文言を見直す**。`statement` に "the maintainer of 校異源氏物語" とあるが、
      本家サイトは作成者を 4 名挙げている。**署名し直しになるので鍵が要る**
- [ ] 前の 55 件（氏名入り）はチェーンに残っている。消せない

## 法規制の講義を踏まえて

- [ ] **【違反】チェーンに個人名が入っている** (p4 / p23)。EDPB の見解は
      「個人情報はチェーンに置かない。平文も、暗号化も、ハッシュもだめ」。
      DDO 55 件の `metadata.author` に `Satoru Nakamura / 中村 覚` を記録済み。**取り消せない**。
      → 今後の DDO には氏名を入れない。`19-reattribute.mjs` は既にその形
- [ ] **p20 / p18「誰が止められるか」に画面で答える**。
      本家 Ocean Market には無い項目だが、この構成では出す価値がある。
      現状: 更新できる=中村 (data NFT の owner)、止められる=誰もいない、
      消せる=誰もいない、儲かる=誰もいない。**鍵は 1Password に 1 本、単独署名**
- [ ] **p21「who can change the supply」** — datatoken の cap を 100 に設定している。
      値上がりの余地はない (無料・売買不能) が、上限を付けたのは設計判断なので記事に書く
- [ ] **FIPPA の確認** (p23「UBC-held data falls under FIPPA」)。
      この実験は個人のもので UBC の保有データではないが、
      史料編纂所の資料を扱う実装に広げるなら所属機関の規程が先に来る
- [ ] `--with-contributors` は使わない（他の 4 名の同意が無い）

## 将来やること

- [ ] 詳細ページに NDL の IIIF / 本家サイト / SPARQL へのリンクを足す（やさしい）
- [ ] お知らせ（Markdown を置くだけ。本家の `pages/[slug].tsx` と同じ形）（やさしい）
- [ ] 可視化ページ（帖ごとの行数・和歌の分布。snapshot から作れる）（ふつう）
- [ ] **IIIF ビューア**（Mirador か OpenSeadragon）。TEI に座標があるので、
      行を選ぶと画像の該当箇所に飛ぶところまで作れる。**いちばん効く**（ふつう）
- [ ] 全文検索（25,065 行。FlexSearch などでビルド時に索引を作れば、サーバ不要のままできる）（むずかしい）
- [ ] SPARQL（本家は外部の Dydra 依存。自前で立てると「サーバ不要」が崩れる）（むずかしい・保留）

## 手順として守ること

- [ ] **撮ったスクリーンショットを必ず開いて見る。**
      `visual.spec.ts` に「目視用の画面を残す」を書いておきながら、一度も開いていなかった。
      テストが通ることと、見て正しいことは別
- [ ] 指摘を受けたら、**まずここに書いてから**着手する
