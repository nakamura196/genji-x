# Genji-X で作業するときの決まり

このファイルは Claude Code が自動で読む。**指針は GUIDELINES.md、残件は TODO.md。**
ここには「どう進めるか」だけを書く。

## 着手の前に

**指摘・依頼を受けたら、まず TODO.md に書く。それから着手する。**

実際に取りこぼした。「ウォレットの接続解除を足します」と言って本家のコードを読み、
そこで別の話題に移り、実装しないまま流れた。同じ指摘を 2 回受けた。
記録せずに次へ進むと、必ず落ちる。

## 終わったと言う前に

### 1. 撮った画像を必ず開く

`e2e/visual.spec.ts` に「目視用の画面を残す」を書いておきながら、
**一度も開かずに「直った」と報告した。** その間、Hero の見出しは本文と同じ大きさで、
検索欄のプレースホルダは隠れ、ロゴは崩れていた。

**テストが通ることと、見て正しいことは別。** Read で PNG を開いて自分の目で見る。

### 2. 「ある」ではなく「見える・効く」を確かめる

`toBeVisible()` は透明でも枠なしでも同色でも通る。
DOM にあることを確かめても、見えていることの確認にはならない。

### 3. 「はみ出し 0」は重なりの確認にならない

横スクロールが出ていないことを測って「狭い画面でも大丈夫」と報告したが、
実際は 400px で見出しと本文が**重なっていた**（`CorpusAnchor` と `1 個` が
同じ位置に描かれていた）。グリッドの列を 1 列に落としたとき、
3 つ目と 4 つ目の要素が同じマスに入っていたため。

`scrollWidth - clientWidth` は、要素どうしの重なりを検出しない。
**矩形が交わっていないかを直接測る。**

```js
const a = x.getBoundingClientRect(), b = y.getBoundingClientRect();
const 重なり = !(a.right <= b.left || b.right <= a.left ||
                 a.bottom <= b.top || b.bottom <= a.top);
```

### 4. 出た値を疑う

参照回数が「どの帖も 5 件」だったのに気づかず、そのまま報告した。
実際は発行時のイベントを数えていて、正しくは 1 件だった。
**同じ値が並んだら、まず自分の集計を疑う。** 別の経路で 1 件だけ突き合わせる。

### 5. 人が見る条件で見る

```
ブラウザで   curl は Origin を送らないので、403 などが再現しない
短いページで フッタの位置
暗い表示で   計算後の色を測る
狭い画面で   480px。横あふれ
```

### 6. 本家を写すときは、先に全部列挙する

部品を 1 つずつ見て書くと必ず漏れる。
`find` でディレクトリ全体を列挙し、**ページの実体（src/pages/）を先に読む**。
部品はその後。

CSS Modules は `cp` でそのまま移植する。見て書き直すと必ずずれる
（角の丸みが本家 0.2rem に対しこちらは 10px だった）。

## 移植したら、使う

1 周目で見つかった最大の問題は「**移植したのに使っていなかった**」だった。
`Page/PageHeader.tsx`、`atoms/Container`、`AssetContent/index.module.css`、
`AssetActions.tsx` は、どこからも import されていなかった。

移植は終わりではない。**実際にそのファイルを import して描画に使うところまでが 1 つの作業。**

## 日本語

`GUIDELINES.md` の用語表に従う。**辞書に無い日本語を作らない。**
読者は研究者で、英語の技術用語のほうが通じる。
「利用の券」「刻む」「工場」のような造語は、かえって分かりにくい。

## 位置づけ

**校異源氏物語テキストDB — web3 版。**
本家サイト (kouigenjimonogatari.github.io) にできないことだけを主張する。
全文検索・SPARQL・IIIF・表示の速さは本家のほうが優れている。そこは正直に書く。

## 開発サーバを止めない

**ポートは 3000 のまま使う。** `PORT=3100` のような別のポートで立てない。

Next 16 は**同じプロジェクトで開発サーバを 2 つ動かせない**（ポートが違っても
「Another next dev server is already running」で拒否される）。
Playwright は `playwright.config.ts` の既定どおり 3000 で探し、
`reuseExistingServer: true` なので**動いていればそれを使う**。

3100 で立てると、テストを走らせるたびに 3000 で立てようとして失敗し、
どちらかを kill することになる。**ユーザーが開いている画面が落ちる。**
実際に 2 回止めて指摘を受けた。

立て直すとき。**`nohup` と `disown` を付ける。**

```
nohup npm run dev > /tmp/genji-dev.log 2>&1 < /dev/null & disown
```

付けないと、こちらのシェルの片付けに巻き込まれて落ちる。実際に落ちて
2 回目の指摘を受けた。しかもそのとき `pgrep -f "next dev"` は
**別プロジェクトのサーバを拾って「動いています」と返した**。
生きているかは必ず HTTP で確かめる:

```
curl -s -o /dev/null -w '%{http_code}\n' http://localhost:3000/ja
```

確認:

```
curl -s -o /dev/null -w '%{http_code}\n' http://localhost:3000/ja
```

## 残件の書き方

**`TODO.md` には未対応のものだけを書く。済んだら `DONE.md` へ移す。**

以前は同じ章に `- [ ]` と `- [x]` が混ざり、章の題も「未対応」なのに中身の半分が
済みだった。**41 件の未対応が 90 件の中に埋もれ、何が残っているか読み取れなかった。**
実際、3 回指摘された項目（ウォレットの接続解除）が 25 行目にありながら残り続けた。

- `TODO.md` は主題ごとに分ける（見た目 / データと帰属 / 法規制 / 将来 / 手順）
- 済んだ項目は**その場でチェックを付けずに、DONE.md へ移す**
- 「済んだもの」という章を TODO.md に作らない。作ると必ずまた混ざる
