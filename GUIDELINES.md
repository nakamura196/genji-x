# Genji-X の作業指針

**この文書は、実際に見逃した不具合から書き起こしたものです。**
一般論は書きません。踏んだことだけを書きます。

## なぜ見逃したか（原因の分類）

ユーザーの指摘で初めて分かった不具合を並べ、なぜ自動では見つからなかったかを書く。

| 見逃したもの | なぜ見つからなかったか |
|---|---|
| Hero の検索欄が見えない | **DOM にあるかしか見ていなかった。** 枠も背景も無く、暗い背景に白い文字が浮くだけだったが、`toBeVisible()` は通る |
| トップがランディングでない | **本家の `src/pages/` を最後まで見ていなかった。** 部品だけ見て、ページの実体を見ていなかった |
| ヘッダの操作が 3 つ抜けていた | 同上。本家の `actions` を**列挙してから**作っていなかった |
| フッタが画面の途中で止まる | 内容の短いページを開いていなかった |
| ダークモードで白いまま | **色を目で見ず、CSS を読んだだけだった。** 計算後の色を測っていなかった |
| 日本語が造語だらけ | **翻訳の質を見る手立てが無かった。** 「利用の券」「刻む」「工場」など、辞書にも他の資料にも無い語を作っていた |
| 参照回数が発行時のイベントを数えていた | **出た数字の妥当性を確かめていなかった。** どの帖も同じ「5 件」だったのに気づかなかった |
| JS が 403 で全部落ちていた | **curl で 200 を見て満足していた。** ブラウザだけが `Origin` を送るので、curl では再現しない |

原因は 4 つに集約される。

1. **存在の確認で止めていた**（見えているか、意味が通るかを見ていない）
2. **本家を列挙せずに、部分だけ見ていた**
3. **出た値の妥当性を疑っていなかった**
4. **人が見る条件で見ていなかった**（ブラウザ / 短いページ / 暗い表示 / 狭い画面）

## 守ること

### 1. 「ある」ではなく「見える・効く」を確かめる

```
だめ   await expect(page.locator('input')).toBeVisible()
よい   枠か背景があること / 文字と背景の対比が 4.5:1 以上 / 実際に打ち込んで結果が変わること
```

`toBeVisible()` は「display:none でない」程度しか見ない。
**透明な背景・枠なし・同色の文字は、すべて「visible」になる。**

### 2. 本家を写すときは、まず列挙する

部品を 1 つずつ見て書くと必ず漏れる。次の順で進める。

```
1. 本家の該当ディレクトリの **全ファイル** を `find` で列挙する
2. ページの実体（src/pages/）を先に読む。部品はその後
3. 列挙した一覧と、こちらの実装を 1 行ずつ突き合わせる
4. 実装する
```

**CSS Modules は `cp` でそのまま移植する。** 見て書き直すと必ずずれる
（角の丸みが本家 0.2rem に対しこちらは 10px だった）。

### 3. 出た数字を疑う

```
参照回数が「どの帖も 5 件」→ おかしい（注文は 1 回しかしていない）
→ 生の RPC で引き直したら 1 件だった。viem の topics 絞り込みが効いていなかった
```

**同じ値が並んだら、まず自分の集計を疑う。**
チェーンから読んだ値は、必ず別の経路（生の RPC / Etherscan / chain-reader）で 1 件だけ突き合わせる。

### 4. 日本語は原語を残し、比喩を作らない

読者は研究者で、英語の技術用語のほうが通じる。**辞書に無い日本語を作ると、かえって分かりにくい。**

| 作ってはいけない | 使う |
|---|---|
| 利用の券 | datatoken |
| 刻む | 記録する（anchor はアンカーする） |
| 住所（CID のこと） | 識別子 / CID |
| 指紋 | ハッシュ |
| 関所 | アクセス制限 |
| 窓口 / 工場 / 殻 | コントラクト / ファクトリ / プロキシ |
| 畳む | 経路をたどって計算する |
| 受領書 | 参照記録 |
| 焼く | バーンする |

規格名（ERC-721 / ERC-20 / data NFT / datatoken / DID / CID / root）は**原語のまま**。
初出で 1 文だけ説明を添える。

### 5. 人が見る条件で見る

必ず次の 4 つで確かめる。

```
ブラウザで  （curl では Origin を送らないので、403 などが再現しない）
短いページで（フッタの位置）
暗い表示で  （計算後の色を測る）
狭い画面で  （480px。横あふれ）
```

### 6. メッセージに山括弧を書かない

next-intl は ICU の書式指定を解釈するので、`<seg>` は「閉じられていないタグ」として落ちる。
`seg 要素` のように書く。

### 7. 移植したものには由来を書く

```
/* Ocean Market / Pontus-X Portal より移植 (Apache-2.0)。
   由来: oceanprotocol/market → deltaDAO/mvg-portal → ClioX-mvg-portal → ここ */
```

## この試作に無い機能（移植しない）

出品(publish)、編集(edit)、購入・価格、Compute-to-Data、プロフィール、
ブックマーク、Onboarding、Faucet、usecases、記事(articles)、Automation。

対応する実体が無いので、移植しても空欄になる。

## 位置づけ

**校異源氏物語テキストDB — web3 版。**

本家サイト（kouigenjimonogatari.github.io）で公開している TEI/XML を、
チェーンと IPFS に載せたもの。**本家にできないことだけ**を主張する。

```
版の固定 / 引用の記録 / 第三者による検証 / 帖ごとの参照数
```

全文検索・SPARQL・IIIF・表示の速さは本家サイトのほうが優れている。**そこは正直に書く。**
